{% extends 'base.html.twig' %}

{% block title %}New Education{% endblock %}
{% block stylesheets %}
<link rel="stylesheet" type="text/css" href="{{ asset('datepicker/css/bootstrap-datepicker.css')}}" />
{% endblock %}
{% block body %}
    <h1>Create new Education</h1>

    {# {{ include('education/_form.html.twig') }} #}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

    <a href="{{ path('education_index') }}">back to list</a>
    
{% endblock %}
{% block javascripts %}
<script src="{{ asset('datepicker/js/bootstrap-datepicker.js')}}" type="text/javascript"></script>
<script>

    $(".datepicker").datepicker( {
        format: "mm-yyyy",
        viewMode: "months", 
        minViewMode: "months"
    });    
</script>
{% endblock %}