{% extends 'base.html.twig' %}

{% block title %}FAQ{% endblock %}

{% block body %}
    <h1>FAQ</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ f_a_q.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ f_a_q.question }}</td>
            </tr>
            <tr>
                <th>Answer</th>
                <td>{{ f_a_q.answer }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('f_a_q_index') }}">back to list</a>

    <a href="{{ path('f_a_q_edit', {'id': f_a_q.id}) }}">edit</a>

    {{ include('faq/_delete_form.html.twig') }}
{% endblock %}
