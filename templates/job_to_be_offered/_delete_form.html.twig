<form method="post" action="{{ path('job_to_be_offered_delete', {'id': job_to_be_offered.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_to_be_offered.id) }}">
    <button class="btn">Delete</button>
</form>
