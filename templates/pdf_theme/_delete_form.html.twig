<form method="post" action="{{ path('pdf_theme_delete', {'id': pdf_theme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pdf_theme.id) }}">
    <button class="btn">Delete</button>
</form>
